{
    "containerDefinitions": [
        {
            "essential": true,
            "image": "<YOUR ECR URL HERE>:${BUILD_VERSION}",
            "name": "${ECS_APP_NAME}",
            "portMappings": [
                {
                    "containerPort": 8888
                }
            ],
            "logConfiguration": {
                "logDriver": "awslogs",
                "options": {
                    "awslogs-group": "ecs-${ECS_APP_NAME}-${ENV}",
                    "awslogs-region": "ap-southeast-2",
                    "awslogs-stream-prefix": "${BUILD_VERSION}"
                }
            }
        }
    ],
    "family": "${ECS_APP_NAME}-${ENV}",
    "volumes": [],
    "memory": "128",
    "cpu": "128"
}
